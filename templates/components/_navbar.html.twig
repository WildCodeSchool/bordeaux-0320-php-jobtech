<nav class="navbar navbar-expand-lg bg-jade d-flex p-0 fixed-top">
    <a class="navbar-brand" href="{{ path('index') }}">
        <img src="{{ asset('/build/images/logo-mini.png') }}" alt="Logo JobTech">
        <span>Job-Tech</span>
    </a>

    <button class="navbar-toggler mr-3" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end bg-jade pl-3 pl-lg-0" id="navbarNav">
        <ul class="navbar-nav d-flex w-100">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('news_list') }}">Actualités</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('offer_list') }}">Offres</a>
            </li>
            <li class="ml-lg-auto mr-3 nav-item">
                <div class="dropdown">
                    <a class="btn btn-primary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Mon espace <i class="far fa-user"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <a class="dropdown-item text-center" href="{{ path('profile') }}">Mon compte</a>
                        <a class="dropdown-item text-center" href="{{ path('message_index') }}">Messagerie</a>
                        {% if is_granted('ROLE_COMPANY') %}
                            <a class="dropdown-item text-center" href="{{ path('offer_new') }}">Ajouter une offre</a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="dropdown-item text-center" href="">Administration</a>
                        {% endif %}
                        <a class="dropdown-item text-center" href="{{ path('logout') }}">Se déconnecter</a>
                    {% else %}
                        <a class="dropdown-item text-center text-gold" href="{{ path('login') }}">Se connecter</a>
                        <p class="text-center title-dropdown pt-1">Créer mon compte</p>
                        <a class="dropdown-item text-center" href="{{ path('register', {'action': 'create_candidate'}) }}">Nouveau candidat</a>
                        <a class="dropdown-item text-center" href="{{ path('register', {'action': 'create_company'}) }}">Nouvelle entreprise</a>
                    {% endif %}
                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>
