<nav class="navbar navbar-expand-lg d-flex p-0 fixed-top">
    <a class="navbar-brand ml-3" href="{{ path('index') }}">
        <img src="{{ asset('/build/images/logo-mini.png') }}" alt="Logo JobTech">
        <span class="font-weight-bolder">Job-Tech</span>
    </a>

    <button class="navbar-toggler mr-3" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"><i class="fas fa-bars"></i></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end bg-white pl-3 pl-lg-0" id="navbarNav">
        <ul class="navbar-nav d-flex align-items-center">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('news_list') }}">Actualités</a>
            </li>
            <li class="nav-item mr-lg-3">
                <a class="nav-link" href="{{ path('offer_list') }}">Offres</a>
            </li>
            <li class="ml-lg-auto mr-5 nav-item d-none d-lg-block dropdown">
                <a class="btn btn-primary dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Mon espace <i class="far fa-user"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <a class="dropdown-item text-center" href="{{ path('profile') }}">Mon compte</a>
                        {% if is_granted('ROLE_CANDIDATE') %}
                            <a class="dropdown-item text-center" href="{{ path('candidate_show_bookmarks') }}">Mes
                                favoris</a>
                        {% endif %}
                        {% if is_granted('ROLE_COMPANY') %}
                            <a class="dropdown-item text-center" href="{{ path('offer_new') }}">Ajouter une offre</a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <a class="dropdown-item text-center" href="{{ path('admin') }}">Administration</a>
                            <a class="dropdown-item text-center" href="{{ path('message_admin_inbox') }}">Messagerie</a>
                        {% else %}
                            <a class="dropdown-item text-center" href="{{ path('message_inbox') }}">Messagerie</a>
                        {% endif %}
                        <a class="dropdown-item text-center" href="{{ path('logout') }}">Se déconnecter</a>
                    {% else %}
                        <a class="dropdown-item text-center text-gold" href="{{ path('login') }}">Se connecter</a>
                        <p class="text-center title-dropdown pt-1">Créer mon compte</p>
                        <a class="dropdown-item text-center"
                           href="{{ path('register', {'action': 'create_candidate'}) }}">Nouveau candidat</a>
                        <a class="dropdown-item text-center"
                           href="{{ path('register', {'action': 'create_company'}) }}">Nouvelle entreprise</a>
                    {% endif %}
                </div>
            </li>

            {# Burger Menu #}
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item d-lg-none">
                    <a href="{{ path('profile') }}" class="nav-link">Mon compte</a>
                </li>
                {% if is_granted('ROLE_CANDIDATE') %}
                    <li class="nav-item d-lg-none">
                        <a class="dropdown-item text-center" href="{{ path('candidate_show_bookmarks') }}">Mes favoris</a>
                    </li>
                {% endif %}
                <li class="nav-item d-lg-none">
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('message_admin_inbox') }}" class="nav-link">Messagerie</a>
                    {% else %}
                        <a href="{{ path('message_inbox') }}" class="nav-link">Messagerie</a>
                    {% endif %}
                </li>
                {% if is_granted('ROLE_COMPANY') %}
                    <li class="nav-item d-lg-none">
                        <a href="{{ path('offer_new') }}" class="nav-link">Ajouter une offre</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item d-lg-none">
                        <a href="{{ path('admin') }}" class="nav-link">Administration</a>
                    </li>
                {% endif %}
                <li class="nav-item d-lg-none">
                    <a href="{{ path('logout') }}" class="nav-link">Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item d-lg-none">
                    <a href="{{ path('login') }}" class="nav-link">Se connecter</a>
                </li>
                <li class="nav-item d-lg-none">
                    <a href="{{ path('register', {'action': 'create_candidate'}) }}" class="nav-link">Nouveau candidat</a>
                </li>
                <li class="nav-item d-lg-none">
                    <a href="{{ path('register', {'action': 'create_company'}) }}" class="nav-link">Nouvelle entreprise</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
