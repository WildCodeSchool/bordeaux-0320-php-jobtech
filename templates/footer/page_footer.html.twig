{% extends 'base.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-primary float-right mt-5 mr-5" href="{{ path('footer_page_edit', {'page': content.identifier}) }}">Editer</a>
    {% endif %}

    <div class="container">
        <h1 class="p-5">{{ content.title }}</h1>
        <div>{{ content.html | raw }}</div>

        <div class="d-flex justify-content-end">
            <a class="text-black hover-orange" href="{{ path('index') }}">Retour à l'accueil <i class="far fa-hand-point-left"></i></a>
        </div>
    </div>

{% endblock %}
